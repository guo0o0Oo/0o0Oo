#include<bits/stdc++.h>
using namespace std;
using ll=long long;
const int maxn=1e6+10;
ll a[maxn],n;
stack<pair<ll,ll> > st;
int main(){
	cin>>n;
	for(int i=1;i<=n;i++){
		cin>>a[i];
	}
	st.push({1,n});
	while(!st.empty()){
		pair<ll,ll> pr=st.top();
		ll mid=
	} 
}
